# express-redis-ratelimit
A lightweight, standalone Express.js middleware that enforces IP-based rate limiting using a Redis-backed sliding window algorithm. It is built as a self-contained, importable package — framework-aware but application-agnostic — that can be dropped into any Express application with a single app.use() call.
